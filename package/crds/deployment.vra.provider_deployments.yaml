---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: deployments.deployment.vra.provider
spec:
  group: deployment.vra.provider
  names:
    categories:
    - crossplane
    - managed
    - vra
    kind: Deployment
    listKind: DeploymentList
    plural: deployments
    singular: deployment
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Deployment is the Schema for the Deployments API. A resource
          that can be used to create a vRealize Automation deployment.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DeploymentSpec defines the desired state of Deployment
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  blueprintContent:
                    description: The content of the the cloud template to be used
                      to request the deployment. Conflicts with blueprint_id and catalog_item_id.
                      The content of the the cloud template to be used to request
                      the deployment.
                    type: string
                  blueprintId:
                    description: The id of the cloud template to be used to request
                      the deployment. Conflicts with blueprint_content and catalog_item_id.
                      The id of the cloud template to be used to request the deployment.
                    type: string
                  blueprintVersion:
                    description: The version of the cloud template to be used to request
                      the deployment. Used only when blueprint_id is provided. The
                      version of the cloud template to be used to request the deployment.
                    type: string
                  catalogItemId:
                    description: The id of the catalog item to be used to request
                      the deployment. Conflicts with blueprint_id and blueprint_content.
                      The id of the catalog item to be used to request the deployment.
                    type: string
                  catalogItemVersion:
                    description: The version of the catalog item to be used to request
                      the deployment. Used only when catalog_item_id is provided.
                      The version of the catalog item to be used to request the deployment.
                    type: string
                  description:
                    description: A human-friendly description. A human-friendly description.
                    type: string
                  expandLastRequest:
                    description: Represents deployment requests.
                    type: boolean
                  expandProject:
                    description: Flag to indicate whether to expand project information.
                      Flag to indicate whether to expand project information.
                    type: boolean
                  expandResources:
                    description: Expanded resources for the deployment. Content of
                      this property will not be maintained backward compatible.
                    type: boolean
                  inputs:
                    additionalProperties:
                      type: string
                    description: Inputs provided by the user. For inputs including
                      those with default values, refer to inputs_including_defaults.
                      Inputs provided by the user. For inputs including those with
                      default values, refer to inputs_including_defaults.
                    type: object
                  name:
                    description: The name of the deployment. The name of the deployment.
                    type: string
                  owner:
                    description: The user this deployment belongs to. At create, the
                      owner is ignored but is used to update during next apply. The
                      user this deployment belongs to.
                    type: string
                  projectId:
                    description: The id of the project this deployment belongs to.
                      The id of the project this deployment belongs to.
                    type: string
                  reason:
                    description: Reason for requesting/updating a blueprint. Reason
                      for requesting/updating a blueprint.
                    type: string
                type: object
              initProvider:
                description: THIS IS A BETA FIELD. It will be honored unless the Management
                  Policies feature flag is disabled. InitProvider holds the same fields
                  as ForProvider, with the exception of Identifier and other resource
                  reference fields. The fields that are in InitProvider are merged
                  into ForProvider when the resource is created. The same fields are
                  also added to the terraform ignore_changes hook, to avoid updating
                  them after creation. This is useful for fields that are required
                  on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them,
                  like an autoscaler.
                properties:
                  blueprintContent:
                    description: The content of the the cloud template to be used
                      to request the deployment. Conflicts with blueprint_id and catalog_item_id.
                      The content of the the cloud template to be used to request
                      the deployment.
                    type: string
                  blueprintId:
                    description: The id of the cloud template to be used to request
                      the deployment. Conflicts with blueprint_content and catalog_item_id.
                      The id of the cloud template to be used to request the deployment.
                    type: string
                  blueprintVersion:
                    description: The version of the cloud template to be used to request
                      the deployment. Used only when blueprint_id is provided. The
                      version of the cloud template to be used to request the deployment.
                    type: string
                  catalogItemId:
                    description: The id of the catalog item to be used to request
                      the deployment. Conflicts with blueprint_id and blueprint_content.
                      The id of the catalog item to be used to request the deployment.
                    type: string
                  catalogItemVersion:
                    description: The version of the catalog item to be used to request
                      the deployment. Used only when catalog_item_id is provided.
                      The version of the catalog item to be used to request the deployment.
                    type: string
                  description:
                    description: A human-friendly description. A human-friendly description.
                    type: string
                  expandLastRequest:
                    description: Represents deployment requests.
                    type: boolean
                  expandProject:
                    description: Flag to indicate whether to expand project information.
                      Flag to indicate whether to expand project information.
                    type: boolean
                  expandResources:
                    description: Expanded resources for the deployment. Content of
                      this property will not be maintained backward compatible.
                    type: boolean
                  inputs:
                    additionalProperties:
                      type: string
                    description: Inputs provided by the user. For inputs including
                      those with default values, refer to inputs_including_defaults.
                      Inputs provided by the user. For inputs including those with
                      default values, refer to inputs_including_defaults.
                    type: object
                  name:
                    description: The name of the deployment. The name of the deployment.
                    type: string
                  owner:
                    description: The user this deployment belongs to. At create, the
                      owner is ignored but is used to update during next apply. The
                      user this deployment belongs to.
                    type: string
                  projectId:
                    description: The id of the project this deployment belongs to.
                      The id of the project this deployment belongs to.
                    type: string
                  reason:
                    description: Reason for requesting/updating a blueprint. Reason
                      for requesting/updating a blueprint.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS A BETA FIELD. It is on by default but can be
                  opted out through a Crossplane feature flag. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.projectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.projectId)
                || (has(self.initProvider) && has(self.initProvider.projectId))'
          status:
            description: DeploymentStatus defines the observed state of Deployment.
            properties:
              atProvider:
                properties:
                  blueprintContent:
                    description: The content of the the cloud template to be used
                      to request the deployment. Conflicts with blueprint_id and catalog_item_id.
                      The content of the the cloud template to be used to request
                      the deployment.
                    type: string
                  blueprintId:
                    description: The id of the cloud template to be used to request
                      the deployment. Conflicts with blueprint_content and catalog_item_id.
                      The id of the cloud template to be used to request the deployment.
                    type: string
                  blueprintVersion:
                    description: The version of the cloud template to be used to request
                      the deployment. Used only when blueprint_id is provided. The
                      version of the cloud template to be used to request the deployment.
                    type: string
                  catalogItemId:
                    description: The id of the catalog item to be used to request
                      the deployment. Conflicts with blueprint_id and blueprint_content.
                      The id of the catalog item to be used to request the deployment.
                    type: string
                  catalogItemVersion:
                    description: The version of the catalog item to be used to request
                      the deployment. Used only when catalog_item_id is provided.
                      The version of the catalog item to be used to request the deployment.
                    type: string
                  createdAt:
                    description: Date when the entity was created. The date is in
                      ISO 6801 and UTC. Date when the entity was created. The date
                      is in ISO 6801 and UTC.
                    type: string
                  createdBy:
                    description: The user the entity was created by. The user the
                      entity was created by.
                    type: string
                  description:
                    description: A human-friendly description. A human-friendly description.
                    type: string
                  expandLastRequest:
                    description: Represents deployment requests.
                    type: boolean
                  expandProject:
                    description: Flag to indicate whether to expand project information.
                      Flag to indicate whether to expand project information.
                    type: boolean
                  expandResources:
                    description: Expanded resources for the deployment. Content of
                      this property will not be maintained backward compatible.
                    type: boolean
                  expense:
                    description: Expense incurred for the deployment.
                    items:
                      properties:
                        additionalExpense:
                          description: Additional expense incurred for the resource.
                          type: number
                        code:
                          description: Expense sync message code if any.
                          type: string
                        computeExpense:
                          description: Compute expense of the entity.
                          type: number
                        lastUpdateTime:
                          description: Last expense sync time.
                          type: string
                        message:
                          description: Expense sync message if any.
                          type: string
                        networkExpense:
                          description: Network expense of the entity.
                          type: number
                        storageExpense:
                          description: Storage expense of the entity.
                          type: number
                        totalExpense:
                          description: Total expense of the entity.
                          type: number
                        unit:
                          description: Monetary unit.
                          type: string
                      type: object
                    type: array
                  id:
                    description: The id of the deployment.
                    type: string
                  inputs:
                    additionalProperties:
                      type: string
                    description: Inputs provided by the user. For inputs including
                      those with default values, refer to inputs_including_defaults.
                      Inputs provided by the user. For inputs including those with
                      default values, refer to inputs_including_defaults.
                    type: object
                  inputsIncludingDefaults:
                    additionalProperties:
                      type: string
                    description: All the inputs applied during last create/update
                      operation, including those with default values. For the list
                      of inputs provided by the user in the configuration, refer to
                      inputs. All the inputs applied during last create/update operation,
                      including those with default values.
                    type: object
                  lastRequest:
                    description: Represents deployment requests.
                    items:
                      properties:
                        actionId:
                          description: Identifier of the requested action.
                          type: string
                        approvedAt:
                          description: Time at which the request was approved.
                          type: string
                        blueprintId:
                          description: The id of the cloud template to be used to
                            request the deployment. Conflicts with blueprint_content
                            and catalog_item_id.
                          type: string
                        cancelable:
                          description: Indicates whether request can be canceled or
                            not.
                          type: boolean
                        catalogItemId:
                          description: The id of the catalog item to be used to request
                            the deployment. Conflicts with blueprint_id and blueprint_content.
                          type: string
                        completedAt:
                          description: Time at which the request completed.
                          type: string
                        completedTasks:
                          description: The number of tasks completed while fulfilling
                            this request.
                          type: number
                        createdAt:
                          description: Date when the entity was created. The date
                            is in ISO 6801 and UTC.
                          type: string
                        details:
                          description: Longer user-friendly details of the request.
                          type: string
                        dismissed:
                          description: Indicates whether request is in dismissed state.
                          type: boolean
                        id:
                          description: The id of the deployment.
                          type: string
                        initializedAt:
                          description: Time at which the request was initialized.
                          type: string
                        inputs:
                          additionalProperties:
                            type: string
                          description: Inputs provided by the user. For inputs including
                            those with default values, refer to inputs_including_defaults.
                          type: object
                        name:
                          description: The name of the deployment.
                          type: string
                        outputs:
                          additionalProperties:
                            type: string
                          description: Request outputs.
                          type: object
                        requestedBy:
                          description: The user that initiated the request.
                          type: string
                        resourceIds:
                          items:
                            type: string
                          type: array
                        status:
                          description: 'Request overall execution status. Supported
                            values: CREATED, PENDING, INITIALIZATION, CHECKING_APPROVAL,
                            APPROVAL_PENDING, INPROGRESS, COMPLETION, APPROVAL_REJECTED,
                            ABORTED, SUCCESSFUL, FAILED.'
                          type: string
                        totalTasks:
                          description: The total number of tasks need to be completed
                            to fulfil this request.
                          type: number
                        updatedAt:
                          description: Last update time (e.g. date format ‘2019-07-13T23:16:49.310Z’).
                          type: string
                      type: object
                    type: array
                  lastUpdatedAt:
                    description: TDate when the entity was last updated. The date
                      is in ISO 6801 and UTC. Date when the entity was last updated.
                      The date is in ISO 6801 and UTC.
                    type: string
                  lastUpdatedBy:
                    description: The user that last updated the deployment. The user
                      that last updated the deployment.
                    type: string
                  leaseExpireAt:
                    description: Date when the deployment lease expire. The date is
                      in ISO 6801 and UTC. Date when the deployment lease expire.
                      The date is in ISO 6801 and UTC.
                    type: string
                  name:
                    description: The name of the deployment. The name of the deployment.
                    type: string
                  orgId:
                    description: The Id of the organization this deployment belongs
                      to. The Id of the organization this deployment belongs to.
                    type: string
                  owner:
                    description: The user this deployment belongs to. At create, the
                      owner is ignored but is used to update during next apply. The
                      user this deployment belongs to.
                    type: string
                  project:
                    description: The project this entity belongs to.
                    items:
                      properties:
                        description:
                          description: A human-friendly description.
                          type: string
                        id:
                          description: The id of the deployment.
                          type: string
                        name:
                          description: The name of the deployment.
                          type: string
                        version:
                          description: Version of the entity, if applicable.
                          type: string
                      type: object
                    type: array
                  projectId:
                    description: The id of the project this deployment belongs to.
                      The id of the project this deployment belongs to.
                    type: string
                  reason:
                    description: Reason for requesting/updating a blueprint. Reason
                      for requesting/updating a blueprint.
                    type: string
                  resources:
                    description: Expanded resources for the deployment. Content of
                      this property will not be maintained backward compatible.
                    items:
                      properties:
                        createdAt:
                          description: Date when the entity was created. The date
                            is in ISO 6801 and UTC.
                          type: string
                        dependsOn:
                          description: A list of other resources this resource depends
                            on.
                          items:
                            type: string
                          type: array
                        description:
                          description: A human-friendly description.
                          type: string
                        expense:
                          description: Expense incurred for the deployment.
                          items:
                            properties:
                              additionalExpense:
                                description: Additional expense incurred for the resource.
                                type: number
                              code:
                                description: Expense sync message code if any.
                                type: string
                              computeExpense:
                                description: Compute expense of the entity.
                                type: number
                              lastUpdateTime:
                                description: Last expense sync time.
                                type: string
                              message:
                                description: Expense sync message if any.
                                type: string
                              networkExpense:
                                description: Network expense of the entity.
                                type: number
                              storageExpense:
                                description: Storage expense of the entity.
                                type: number
                              totalExpense:
                                description: Total expense of the entity.
                                type: number
                              unit:
                                description: Monetary unit.
                                type: string
                            type: object
                          type: array
                        id:
                          description: The id of the deployment.
                          type: string
                        name:
                          description: The name of the deployment.
                          type: string
                        propertiesJson:
                          description: List of properties in the encoded JSON string
                            format.
                          type: string
                        state:
                          description: The current state of the resource. Supported
                            values are PARTIAL, TAINTED, OK.
                          type: string
                        syncStatus:
                          description: The current sync status. Supported values are
                            SUCCESS, MISSING, STALE.
                          type: string
                        type:
                          description: Type of the resource.
                          type: string
                      type: object
                    type: array
                  status:
                    description: 'Request overall execution status. Supported values:
                      CREATED, PENDING, INITIALIZATION, CHECKING_APPROVAL, APPROVAL_PENDING,
                      INPROGRESS, COMPLETION, APPROVAL_REJECTED, ABORTED, SUCCESSFUL,
                      FAILED. The status of the deployment with respect to its life
                      cycle operations.'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
